version: '3'

services:
  flask-app:
    build: .
    ports:
      - "5009:5009"
  
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "185:80"
    depends_on:
      - flask-app
